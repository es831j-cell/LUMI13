<Window x:Class="Lumi.DockingStation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Lumi Docking Station" Width="1380" Height="860"
        MinWidth="1100" MinHeight="700" WindowStartupLocation="CenterScreen"
        Loaded="Window_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="74"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="36"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#11161D" BorderBrush="#29313D" BorderThickness="0,0,0,1">
            <Grid Margin="22,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="LUMI // DOCKING STATION" FontSize="22" FontWeight="SemiBold" Foreground="White"/>
                    <TextBlock Text="Command deck · wired phone dock · local build control" FontSize="12" Foreground="#93A0B2" Margin="0,4,0,0"/>
                </StackPanel>
                <Border Grid.Column="1" VerticalAlignment="Center" Background="#17221E" BorderBrush="#2D5F4D" BorderThickness="1" CornerRadius="14" Padding="12,6">
                    <TextBlock x:Name="TopConnectionText" Text="PHONE: CHECKING" Foreground="#9FE7C4" FontSize="11" FontWeight="SemiBold"/>
                </Border>
            </Grid>
        </Border>

        <TabControl Grid.Row="1" Margin="18">
            <TabItem Header="Command Deck">
                <Grid Margin="18">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2.15*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Grid Grid.Column="0" Margin="0,0,10,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <StackPanel>
                            <TextBlock Text="Talk to Lumi's workstation" FontSize="27" FontWeight="SemiBold"/>
                            <TextBlock Text="Type an instruction below. Workstation commands execute locally. Other instructions are recorded and can be answered through an enabled AI provider." Foreground="{StaticResource MutedBrush}" TextWrapping="Wrap" Margin="0,6,0,14"/>
                        </StackPanel>
                        <TextBox x:Name="CommandTranscriptBox" Grid.Row="1" IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" FontFamily="Consolas" Padding="12" Text="Lumi Docking Station 0.2.0 online.&#x0a;Type 'help' for commands.&#x0a;"/>
                        <Grid Grid.Row="2" Margin="0,12,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="CommandInputBox" MinHeight="48" AcceptsReturn="True" TextWrapping="Wrap" VerticalContentAlignment="Center" Padding="10,7" KeyDown="CommandInput_KeyDown" ToolTip="Enter sends. Shift+Enter adds a new line."/>
                            <Button x:Name="SendCommandButton" Grid.Column="1" Content="Send Instruction" Click="SendCommand_Click" Margin="10,0,0,0" Padding="18,8"/>
                        </Grid>
                    </Grid>

                    <StackPanel Grid.Column="1" Margin="10,0,0,0">
                        <Border Background="{StaticResource PanelBrush}" BorderBrush="#29313D" BorderThickness="1" CornerRadius="10" Padding="18" Margin="0,0,0,12">
                            <StackPanel>
                                <TextBlock Text="PHONE DOCK" FontSize="19" FontWeight="SemiBold"/>
                                <TextBlock x:Name="PhoneStateText" Text="Checking for ADB..." Foreground="#FFD48A" Margin="0,10,0,2"/>
                                <TextBlock x:Name="PhoneDetailText" Text="No device information yet." Foreground="{StaticResource MutedBrush}" TextWrapping="Wrap" Margin="0,3,0,12"/>
                                <WrapPanel>
                                    <Button Content="Connect / Refresh" Click="RefreshPhone_Click"/>
                                    <Button Content="Open Lumi" Click="OpenLumi_Click"/>
                                </WrapPanel>
                            </StackPanel>
                        </Border>

                        <Border Background="{StaticResource PanelBrush}" BorderBrush="#29313D" BorderThickness="1" CornerRadius="10" Padding="18" Margin="0,0,0,12">
                            <StackPanel>
                                <TextBlock Text="DEVELOPMENT" FontSize="19" FontWeight="SemiBold"/>
                                <WrapPanel Margin="0,10,0,0">
                                    <Button Content="Build" Click="Build_Click"/>
                                    <Button Content="Diagnostics" Click="Diagnostics_Click"/>
                                    <Button Content="Build + Install" Click="Deploy_Click"/>
                                </WrapPanel>
                                <WrapPanel Margin="0,8,0,0">
                                    <Button Content="Android Studio" Click="OpenAndroidStudio_Click"/>
                                    <Button Content="Workspace" Click="OpenWorkspace_Click"/>
                                </WrapPanel>
                            </StackPanel>
                        </Border>

                        <Border Background="#191525" BorderBrush="#4C3B78" BorderThickness="1" CornerRadius="10" Padding="18">
                            <StackPanel>
                                <TextBlock Text="COMMANDS" FontSize="18" FontWeight="SemiBold" Foreground="#C8B7FF"/>
                                <TextBlock Text="help · check phone · status · diagnostics · build · deploy · android studio · workspace" Foreground="#B7ABC9" TextWrapping="Wrap" Margin="0,8,0,0"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>
            </TabItem>

            <TabItem Header="API Keys">
                <Grid Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <StackPanel>
                        <TextBlock Text="Encrypted Provider Vault" FontSize="24" FontWeight="SemiBold"/>
                        <TextBlock Text="Keys are encrypted to this Windows user with DPAPI. No key is stored in source or diagnostics." Foreground="{StaticResource MutedBrush}" Margin="0,6,0,16"/>
                    </StackPanel>
                    <Border Grid.Row="1" Background="{StaticResource PanelBrush}" BorderBrush="#29313D" BorderThickness="1" CornerRadius="10" Padding="18" Margin="0,0,0,12">
                        <Grid>
                            <Grid.ColumnDefinitions><ColumnDefinition Width="160"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                            <TextBlock Text="Groq API key" VerticalAlignment="Center"/>
                            <PasswordBox x:Name="GroqKeyBox" Grid.Column="1" MinWidth="480" Padding="8"/>
                            <Button Grid.Column="2" Content="Save Groq" Click="SaveGroq_Click" Margin="10,0,0,0"/>
                        </Grid>
                    </Border>
                    <Border Grid.Row="2" Background="{StaticResource PanelBrush}" BorderBrush="#29313D" BorderThickness="1" CornerRadius="10" Padding="18">
                        <Grid>
                            <Grid.ColumnDefinitions><ColumnDefinition Width="160"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                            <TextBlock Text="OpenRouter key" VerticalAlignment="Center"/>
                            <PasswordBox x:Name="OpenRouterKeyBox" Grid.Column="1" MinWidth="480" Padding="8"/>
                            <Button Grid.Column="2" Content="Save OpenRouter" Click="SaveOpenRouter_Click" Margin="10,0,0,0"/>
                        </Grid>
                    </Border>
                </Grid>
            </TabItem>

            <TabItem Header="Diagnostics">
                <Grid Margin="20">
                    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                    <TextBlock Text="Docking Station Diagnostics" FontSize="22" FontWeight="SemiBold"/>
                    <TextBox x:Name="DiagnosticsBox" Grid.Row="1" Margin="0,16,0,0" IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" FontFamily="Consolas" Padding="12" Text="Docking Station initialized.&#x0a;"/>
                </Grid>
            </TabItem>
        </TabControl>

        <Border Grid.Row="2" Background="#11161D" BorderBrush="#29313D" BorderThickness="0,1,0,0">
            <Grid Margin="18,0">
                <TextBlock x:Name="StatusText" Text="Starting..." VerticalAlignment="Center" Foreground="#9FE7C4"/>
                <TextBlock Text="Lumi Docking Station 0.2.0" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="#748093"/>
            </Grid>
        </Border>
    </Grid>
</Window>
